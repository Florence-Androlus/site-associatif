<!-- HEADER -->
<header>
{% if not app.user %}
    <h6><a href="{{path('security_login')}}"><i class="fas fa-power-off"></i> Connexion</a>
    <br><a href="{{path('security_registration')}}">inscription</a></h6>
{% else %}  
    <h6><a href="{{path('security_logout')}}"><i class="fas fa-power-off"></i> Deconnexion</a> 
    <br>Bonjour : {{ app.user.firstname }} {{ app.user.username }} 
    <br>Status : {{ app.user.roles[0] }} </h6>
{% endif %}   



<!--a href="{{path('home')}}"><img src="{{ asset('assets/images/home1-bg.jpg') }}"-->
<h3 style="visibility: hidden">x</h3></!--a>
<h1 class="text-center">Site Officiel de l'association</h1>

        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container-fluid">
                <a type="button" class="btn btn-primary" style="color: #0f5f9c" href="{{path('home')}}">Accueil</a>
                <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse"  data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                    <div class="navbar-collapse collapse"  id="navbarResponsive">
                        <ul class="navbar-nav">
                            <li class="nav-item dropdown">
                            <a type="button" class="btn btn-primary dropdown-toggle" style="color: #0f5f9c" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Action</a>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="{{path('election.index', {'annee': 2020}) }}">2020</a>
                                    <a class="dropdown-item" href="{{path('election.index', {'annee': 2021}) }}">2021</a>
                                </div>
                            </li>
                            
                            {% if  app.session %}
                            {% if app.user %} 
                            {% if  is_granted('ROLE_BENEVOLE') %}
                            <li class="nav-item dropdown">
                            <a type="button" class="btn btn-primary dropdown-toggle" style="color: #0f5f9c" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Liste électorale</a>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="{{path('liste.electorale.index', {'annee': 2020}) }}">2020</a>
                                    <a class="dropdown-item" href="{{path('liste.electorale.index', {'annee': 2021}) }}">2021</a>
                                </div>
                            </li>
                            <li class="nav-item dropdown">  
                                <a type="button" class="btn btn-primary dropdown-toggle" style="color: #0f5f9c" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Contact</a>
                                    <div class="dropdown-menu">
                                        <a class="dropdown-item" href="{{path('CS.index', {'annee': 2020})}}">CS2020 </a>
                                        <a class="dropdown-item" href="{{path('adhesionpbl.index')}}">Adhesion PBL</a>
                                    </div>
                            </li>
                            {% endif %}
                            {% if  is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a type="button" class="btn btn-primary" style="color: #0f5f9c" href="{{path('admin.user.index')}}">Adherents</a>
                            </li>
                            <li class="nav-item">
                                <a type="button" class="btn btn-primary" style="color: #0f5f9c" href="{{path('admin.role.index')}}">Roles</a>
                            </li>
                            {% endif %}
                            <li class="nav-item">
                                <a type="button" class="btn btn-primary" style="color: #0f5f9c" href="{{path('user')}}">Profil</a>
                            </li>
                        </ul>
                    </div>
                {% endif %}
                {% endif %}
            </div>
            {% if  annee is defined %}
                <div class="container-fluid" style="justify-content: flex-end; display: none;{% if '/Elections/' in  app.request.requestUri %} display: block;{% endif %}">
                    <ul class="navbar-nav">
                        <li class="nav-item dropdown">
                        <a type="button" class="btn btn-primary" style="color: #0f5f9c"  href="{{path('tracts', {'annee': annee})}}" role="button">Retrouvez nos documents thématiques</a>
                        </li>
                        <li class="nav-item dropdown">
                        <a type="button" class="btn btn-primary" style="color: #0f5f9c"  href="{{path('videos', {'annee': annee})}}" role="button">Retrouvez nos videos</a>
                        </li>
                        <li class="nav-item dropdown">
                        <a type="button" class="btn btn-primary" style="color: #0f5f9c"  href="{{path('lives', {'annee': annee})}}" role="button">Retrouvez nos lives</a>
                        </li>
                    </ul>
                </div>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <div class="container-fluid" style="justify-content: flex-end; display: none;{% if '/admin/' in  app.request.requestUri %} display: block;{% endif %}">
                    <ul class="navbar-nav">
                        <li class="nav-item dropdown">
                        <a type="button" class="btn btn-primary" style="color: #0f5f9c"  href="{{path('tables')}}" role="button">Tables</a>
                        </li>
                        <li class="nav-item dropdown">
                        <a type="button" class="btn btn-primary" style="color: #0f5f9c"  href="" role="button">Evenements</a>
                        </li>
                    </ul>
                </div>
            {% endif %}
        </nav>
</header>